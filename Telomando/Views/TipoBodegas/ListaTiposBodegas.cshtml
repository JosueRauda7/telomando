@model List<Telomando.Models.TipoBodega>
@{
    ViewData["Title"] = "Tipos de Bodegas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h1 class="display-4">Tipos de Bodegas</h1>

</div>

<div class="card">
    <div class="card-header">
        <h5>Tipos de Bodegas</h5>
    </div>
    <div class="card-body">
        <a class="btn-success btn-sm" asp-action="TipoBodega_Detalle" asp-controller="TipoBodegas" asp-route-idTipoBodega="0">Crear Nuevo</a>
        <hr />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Tipo de Bodega</th>
                    <th>Fecha de Creacion</th>
                    <th>Activo</th>
                    <th>Eliminado</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nombre</td>
                        <td>@item.FechaRegistro</td>
                        <td>@item.Activo</td>
                        <td>@item.Eliminado</td>

                        <td>
                            <a class="btn btn-primary btn-sm" asp-action="TipoBodega_Detalle" asp-controller="TipoBodegas" asp-route-idTipoBodega="@item.Idtipobodega">Editar</a>
                            <a class="btn btn-danger btn-sm" asp-action="Eliminar" asp-controller="TipoBodegas" asp-route-idTipoBodega="@item.Idtipobodega">Eliminar</a>
                        </td>

                    </tr>

                }
            </tbody>
        </table>
    </div>

</div>

