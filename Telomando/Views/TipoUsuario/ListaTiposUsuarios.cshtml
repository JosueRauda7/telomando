@model List<Telomando.Models.TipoUsuario>
@{
    ViewData["Title"] = "Tipos de Usuarios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h1 class="display-4">Tipos de Usuarios</h1>

</div>

<div class="card">
    <div class="card-header">
        <h5>Tipos de Usuarios</h5>
    </div>
    <div class="card-body">
        <a class="btn-success btn-sm" asp-action="TiposUsuarios_Detalle" asp-controller="TipoUsuario" asp-route-idtipousuario="0">Crear Nuevo</a>
        <hr />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Fecha de Creacion</th>
                    <th>Activo</th>
                    <th>Eliminado</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nombre</td>
                        <td>@item.FechaRegistro</td>
                        <td>@item.Activo</td>
                        <td>@item.Eliminado</td>

                        <td>
                            <a class="btn btn-primary btn-sm" asp-action="TiposUsuarios_Detalle" asp-controller="TipoUsuario" asp-route-idTipoUsuario="@item.Idtipousuario">Editar</a>
                            <a class="btn btn-danger btn-sm" asp-action="Eliminar" asp-controller="TipoUsuario" asp-route-idTipoUsuario="@item.Idtipousuario">Eliminar</a>
                        </td>

                    </tr>

                }
            </tbody>
        </table>
    </div>

</div>

